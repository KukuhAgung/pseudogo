constant NMAX : integer = 6
type arr_int : array [1..NMAX] of integer

procedure SelectionSort(inout arr : arr_int, in N : integer)
Kamus:
    i, j, minIdx, temp : integer
Algoritma:
    for i <- 1 to N - 1 do
        minIdx <- i
        for j <- i + 1 to N do
            if (arr[j] < arr[minIdx]) then
                minIdx <- j
            end if
        end for

        temp <- arr[i]
        arr[i] <- arr[minIdx]
        arr[minIdx] <- temp
    end for
end procedure

Program TestSelectionSort
Kamus:
    data : arr_int
    i : integer
Algoritma:
    data[1] <- 64
    data[2] <- 25
    data[3] <- 12
    data[4] <- 22
    data[5] <- 11
    data[6] <- 90
    SelectionSort(data, 6)
    for i <- 1 to 6 do
        output("data[", i, "] = ", data[i])
    end for
